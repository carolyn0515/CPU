# CPU
A modular CPU simulator project that models core computer architecture components such as the Control Unit, ALU, Memory, and Registers. It simulates the instruction fetch–decode–execute cycle and provides a structured environment for learning CPU design and system execution flow.
